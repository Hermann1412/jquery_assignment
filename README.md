# jquery_assignment

Based on the HTML given, improving it to meet the following requirements 

If any of the username, email, password or confirmed password is empty, then 

Change the border color to red 

Display a small text under the field as “this filed must not be empty” 

At least one of the Interesting Topics choices must be checked, otherwise display a small red text besides the label as “At least one topic must be selected” 

If the Gender drop down selection must not be “—” (the first option), display a red small text beside the label as “please choose your gender” 

When clicking on Submit button, check the confirmed password. If confirmed password value does not match the password value, then 

Change the confirmed password border color to red 

Display a small text under confirmed password field as “confirmed password mismatched the password” 

Note that you can modify the HTML freely as well as create any number of corresponding files. 

Explicitly importing CSS and JavaScript is preferred, but implementing it internally has no penalty. 
